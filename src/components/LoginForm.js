import React from 'react';
import PropTypes from 'prop-types';

LoginForm.propTypes = {
  
};

function LoginForm(props) {
  return (
    <div>
      <label>Username: <input onChange={} value={} name='username' type='text'/></label>
      <label>Password: <input onChange={} value={} name='username' type='password'/></label>
    </div>
  );
}

export default LoginForm;